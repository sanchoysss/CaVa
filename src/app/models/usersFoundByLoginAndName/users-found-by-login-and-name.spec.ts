import { UsersFoundByLoginAndName } from './users-found-by-login-and-name';

describe('UsersFoundByLoginAndName', () => {
  it('should create an instance', () => {
    expect(new UsersFoundByLoginAndName()).toBeTruthy();
  });
});
