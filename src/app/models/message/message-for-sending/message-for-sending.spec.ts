import { MessageForSending } from './message-for-sending';

describe('MessageForSending', () => {
  it('should create an instance', () => {
    expect(new MessageForSending()).toBeTruthy();
  });
});
